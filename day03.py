# pip is used to install the required modules
# modules are used to use the codes written by others
# pip install pandas
# pip install scikit-learn
# build in modules are already installed ones
# External Modules ae the ones that are not built in
import pandas as pd
import sklearn as sk